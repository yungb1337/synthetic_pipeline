# Parser Module (Extraction -> DOM) — pinned deps
# Python >= 3.11

# PDF text/layout backend
pymupdf>=1.24

# image reading (OCR input, TIFF, PNG, JPEG)
pillow>=10

# XLSX / spreadsheet
openpyxl>=3.1

# DOM models
pydantic>=2.7

# tests
pytest>=8

# --- on-prem OCR (optional runtime; lazily imported so the parser works without it) ---
# Modern `rapidocr` (PP-OCRv6), the same package+models Docling's OCR uses, so
# the pipeline has a single OCR engine. (Legacy rapidocr_onnxruntime removed.)
rapidocr>=3.9,<4
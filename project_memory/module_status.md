---
name: module-status
description: Live status of platform modules and what is being worked on now
metadata:
  type: project
---

# Module Status

**Phase:** Module #1 — the Parser ("Document / Extraction Pipeline") — design in progress, not yet coded.

## The intended build order (dependency-first, per brief)
1. **Parser / Document Extraction Pipeline** ← **NOW**
2. Text Normalization & Cleaning
3. Semantic Chunking
4. Embeddings
5. Knowledge extraction (Entity/Rel) → Ontology mapping → KG
6. Validation framework (multi-stage)
7. Generation (Prompt builder + LLM + multi-generator)
8. Dataset versioning / lineage / compiler
9. Multi-tenancy, security, governance
10. Delivery APIs / dashboard / observability

## Current status
- [x] Read source material (SYN1-4). Papers-pending (4 PDFs).
- [x] Project memory scaffold (this + master_context + architecture_decisions + reading_notes + MEMORY index).
- [x] Parser module design spec → `docs/parser-module-spec.md`
- [x] **Confirm tech stack** (PyMuPDF · RapidOCR · Extraction→DOM · Store-abstraction)
- [x] **Implement parser (Extraction→DOM)** — venv + all modules
- [x] **Tests green (7)** — detection/CSV/MD/PDF/idempotency/unsupported/store
- [x] **Checkpoint 001** → `checkpoints/checkpoint_001.md`
- [x] **Module #2 Normalizer** implemented+tested → `app/normalizer/`, `docs/normalizer-module-spec.md` (18 tests green)
- [x] **Batch/scale layer** → `app/processing/` + batched OCR + batching-capable embeddings seam → `docs/scale-batch-spec.md` (24 tests green; ~2ms/doc, incremental manifest verified)
- [x] **Real local GPU embeddings** → `SentenceTransformerEmbedder` = **`BAAI/bge-m3` (1024-dim, multilingual)** on RTX 3050 (torch 2.13+cu126, sentence-transformers 5.6, fp16 to fit VRAM); model in `models/bge-m3` via `scripts/download_models.py`; `factory.default_embedder` auto GPU/CPU, Dummy fallback
- [x] **Git initialized** (commit f5a72ec) — source/docs/memory/tests tracked; `.venv`, `models/`, caches ignored
- [x] **Audit run `run-2026-08-04-audit` COMPLETE (2026-08-04)** — Architecture + Quality reviewers both **PASS** after 2 fix rounds; suite **27 → 31 green**; similarity signal clean (no pairs ≥ 0.40). Key hardening: batch-layer self-deadlock fixed, `.txt` loader fixed, content-hash image keys, versioned DOM storage (`dom/{doc_id}/dom-v{version}.docJSON`), idempotent normalizer (`_WS_RE`), manifest dirty-flag. 6 design items deferred → tracked in [[questions]].
- [x] **Docling backend run `run-2026-08-04-docling` COMPLETE (2026-08-04)** — ADR-007: **Docling 2.118.0 integrated as a GATED layout/table engine** (`ParserConfig.layout_backend`: `"native"` default, `"docling"` opt-in, auto-engages where layout is required). On the Docling path it **replaces** the heuristic reading order + PyMuPDF `find_tables`; DOM/harness/events/storage/trust boundary untouched. `app/parser/loaders/docling_loader.py` (lazy engine, on-prem cache `models/docling` ~506M git-ignored). Provenance records `docling_version`/`layout_model`; reading order authoritative on that path. Env fixes baked in: `TORCHDYNAMO_DISABLE` (no Triton on Windows/Py3.14), `iterate_items()` tuple unpacking. Suite **35 passed / 1 skipped** green; both reviewers PASS.
- [x] **Semantic Chunking run `run-2026-08-04-chunking` COMPLETE (2026-08-05)** — new **`app/chunking/`** module (Module #3): DOM-anchored **content-addressed chunks** (~400-token target / 2048 hard cap, 48-token heading-seam overlap), **`ChunkStore`** seam (interface-only — the **retrieval-grounding seam**), **`ChunkEmbedPipeline`** (never-embed-twice keyed on `chunk_id`, token-budget batching ≤16k tokens / ≤32 texts), **BGE-M3 wired as the product embedder** with identity-bearing `name` (`BAAI/bge-m3@<rev>-fp16`), batch defaults lowered 128/64 → 32 (embedding factory + processing config + `SentenceTransformerEmbedder` ctor). ADR-009 + ADR-010; fix round 1 folded a positional `piece_index` into oversized-piece identity (chunk_id collision fix). Suite **99 passed / 1 skipped** (baseline 34/1). Retrieval-grounding seam delivered — Module #4 (retrieval) is next.
- [x] **Intelligent Document Router run `run-2026-08-06-router` COMPLETE (2026-08-10)** — new **`app/routing/`** module (ADR-011): **FastInspector** (PyMuPDF open-without-render feature pass), **9 pluggable detectors** (metadata/text/image/layout/ocr/table/form/reading_order/font; one concern each, failure-isolated), an **absolute-sum complexity scorer** (`scoring.py` — the weighted sum is *not* normalized; the `RoutingConfig` weights ARE the band map), and a **3-band policy** (`0–30 native / 31–60 enrichment / 61–100 docling`) with **conservative escalate-on-low-confidence** (native→enrichment→docling, never downgrade). `RoutingDecision` persisted additively into `Provenance.routing` — **versioned** (`router_v/policy_v/scoring_v` + per-detector), so "why Docling six months ago?" is answerable (§10). **ADR-012** closes the deferred "PDF OCR fallback for scanned pages" item: the Enrichment band = native extraction + OCR of no-text-block pages (reuses `ocr.ocr_bytes`; no new dependency). ADR-007 amendment flips default `layout_backend` `"native"`→`"auto"` (manual `"native"`/`"docling"` overrides preserved). **Calibrated 2026-08-10 against the real `test_cases` corpus**: scans→enrichment(OCR), complex-academic→docling, simple→native; calibrated weights live in `RoutingConfig` (`app/routing/config.py`). Suite **159 passed / 1 skipped**. **Process note:** the engineer subagent hit repeated server errors and the **orchestrator completed the build + CALIBRATION + fix round directly** (scoring.py absolute-sum change, calibrated weights in config.py, corpus-regression test skip-guard, find_tables page-cap, table negative-vs-missing were all orchestrator-applied) — see `checkpoints/run/run-2026-08-06-router/` (architecture approval + per-gate artifacts).
- [x] **Docling OCR via RapidOCR (2026-08-11, user request)** — Docling's own OCR stage (`RapidOcrOptions`) runs **RapidOCR/onnxruntime, the same engine family as `app/parser/ocr.py`**. `ParserConfig.docling_ocr: bool = True` (default) now builds the Docling pipeline with `do_ocr=True` + `RapidOcrOptions(mode=OcrMode.DEFAULT, scale=2.0)` (on-demand, conservative scale). ADR-012 addendum. **Verified:** a previously-empty scanned ticket through Docling → 66 ORC'd blocks (heading/paragraph/list_item, authoritative reading order); a docling-routed text paper still parses cleanly (no OOM); full suite 159 green. In automatic routing, fully-scanned docs still go to **enrichment** (cheaper RapidOCR); Docling OCR fires mainly for docling-routed docs with low-text pages. Caveat: Docling OCR is heavier (~42s/2 scanned pages); very large docs with many low-text pages remain memory-heavy (on-demand mitigates, not a hard cap).
- [x] **OCR unified on one RapidOCR v6 engine (2026-08-11)** — `app/parser/ocr.py` moved from legacy `rapidocr_onnxruntime` (PP‑OCRv4) to the modern `rapidocr` package (PP‑OCRv6), **the exact same package+model files Docling's OCR uses** — two different RapidOCR versions for the same task are now one v6. `_extract_results` handles the new `RapidOCROutput` (`.txts/.boxes/.scores`) with a legacy fallback. Verified: prescriptions 61/28 lines (v4 was 36/23), scanned ticket → 112 OCR blocks, full suite 159 green. Docling's layout/reading order is produced by its layout model, not OCR — this change only affects text recovery. (ADR-012 2nd addendum.)
- [x] **Page-centric execution model run `run-2026-08-19-page-centric` COMPLETE (2026-08-19)** — **Module #1 (`app/parser`) execution model REDESIGNED to page-centric** (ADR-013): the **page is now the fundamental processing AND durable-storage unit** while the **document stays the orchestration unit**. New additive modules under `app/parser/`: `source.py` (`SourceScan`, establishes `expected_page_set` from `fitz len(pdf)` *before* paging), `engines/{base,native_pdf,enrichment,heavy_docling,image,simple}.py` (each `PageEngine` → `PageResult`), `page_result.py` (`PageResult`/`PageStatus` — additive, no `Document`/`Page`/`Block` schema change), `planner.py` (`ExecutionPlan`/`PageWorkItem` + resume), `storage_pages.py` (`PageStore` + per-doc `Ledger` (`manifest/<doc_id>/plan.json`)), `scheduler.py` (`ResourceGovernor` derives `heavy_concurrency=f(ram,F,headroom,gpu)` from measured RAM; `native_pool` wide `ThreadPoolExecutor` + bounded `heavy_pool` `ProcessPoolExecutor`), `assembler.py` (`DocumentValidator` gate: assembled_set==expected_set else re-enqueue→dead-letter with explicit `actual_vs_expected`). **Router unchanged (ADR-011)** — decision-only, band applied page-by-page. Docling now called per page via `page_range=(p,p)` (peak C++ heap bounded to one page); `docling_loader.convert_path` reads `ConversionResult.status`+`errors` so empty stubs can never masquerade as success — **the silent-loss guarantee is now enforced**. `Extractor.extract()` signature + `FilesystemStore` raw/dom/images layout unchanged; `pages/`+`manifest/` are additive. **Verified:** pytest **204 passed / 1 skipped / 0 failed** (68.4s); clean corpus (12 PDFs + 3 images) **15/15 parsed, 0 `std::bad_alloc`, 0 silent page loss** (`actual==expected`, `missing/dead/failed` empty, `assembly.status==ok`); both reviewers (Gate 5 Architecture + Gate 6 Quality) **PASS** after 1 fix loop. **GPU path untested on this CPU box** (`gpu_free/gpu_per_job` dormant; `PYTORCH_CUDA_ALLOC_CONF` set but no CUDA) — see [[questions]].
- [ ] Archive the academic-PDF read (re-dispatch) → ground Ontology/KG/Ontology decisions

## Next module
**Module #3 — Semantic Chunking** (own module, own spec), then wire the real embedder into the chunk→embed pipeline.

**Module #4 — retrieval next** (updated 2026-08-05, run-2026-08-04-chunking COMPLETE): `ChunkStore.iter_*` (`app/chunking/store.py`) is the retrieval seam; a vector store (pgvector/Qdrant) + hybrid BM25+dense behind it is a future ADR. Documented next steps from this run: atomic table/figure-caption chunks, parent-child context injection (both schema-reserved).

## Blocked by
Nothing for the parser itself. Folder rename (22_07 → synthetic_pipeline) blocked until editor releases CWD.
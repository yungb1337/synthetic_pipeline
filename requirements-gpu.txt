# GPU / local-model extras for the R5 embedding path.
# These install the CUDA-enabled PyTorch + sentence-transformers so a real,
# local, open-source embedding model runs on your NVIDIA GPU.
#
# NOTE: do NOT put these in requirements.txt — a plain `pip install
# sentence-transformers` pulls the CPU-only `torch` from PyPI. Install this
# file instead (the CUDA wheel bundles the CUDA runtime):
#
#     pip install --index-url https://download.pytorch.org/whl/cu126 torch
#     pip install sentence-transformers

# Runtime embedding: real local model (BAAI/bge-small-en-v1.5 by default),
# GPU-aware via PyTorch CUDA.
# torch==2.13.0+cu126        (installed from the cu126 index above)
sentence-transformers>=5.6
transformers>=5.14